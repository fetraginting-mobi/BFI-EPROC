IF COL_LENGTH('dbo.INVENTORY_MUTATION_HEADER', 'is_upload') IS NULL
BEGIN
	ALTER TABLE [dbo].[INVENTORY_MUTATION_HEADER]
	ADD [is_upload] [bit] NOT NULL CONSTRAINT DF_INVENTORY_MUTATION_HEADER_is_upload DEFAULT 0;
END
GO
